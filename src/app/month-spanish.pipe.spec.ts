import { MonthSpanishPipe } from './month-spanish.pipe';

describe('MonthSpanishPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthSpanishPipe();
    expect(pipe).toBeTruthy();
  });
});
